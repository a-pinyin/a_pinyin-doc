{
  "name": "a_pinyin_test",
  "version": "0.1.0",
  "private": true,
  "description": "test only",
  "author": "sceext",
  "scripts": {
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "test": "npm run fmt:check"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  },
  "engines": {
    "node": ">=17.0",
    "npm": ">=8.0"
  }
}
